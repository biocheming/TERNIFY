#pragma once
#include <map>
#include <string>
#include <cmath>  // 添加这行来使用 pow() 函数

// 声明外部变量
extern std::map<std::string, double> paras;
extern std::map<std::string, int> hbondtypes;
extern std::map<std::string, double> atomtypes;
